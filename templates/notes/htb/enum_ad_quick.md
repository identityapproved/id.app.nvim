## Enumeration - AD Quick

- domain:
- dc:
- users/groups:

```bash
# if you have creds, swap values
crackmapexec smb <ip> -u '<user>' -p '<pass>' --users
crackmapexec ldap <ip> -u '<user>' -p '<pass>'
```

### Notes

* kerberoast candidates:
* ASREPRoast candidates:
* bloodhound planned: <yes/no>
